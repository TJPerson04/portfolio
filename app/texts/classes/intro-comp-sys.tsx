export function renderIntroCompSys() {
  return (
    <div className="intro-comp-sys">
      <p>
        In this class I learned about the process that goes on starting from
        high-level C code all the way down to the transistors of the CPU. This
        class taught me all about how computers compile code and then how the
        CPU actually handles each line in the fetch/execute cycle. I learned{" "}
        <a
          className="link"
          href="https://en.wikipedia.org/wiki/Little_Computer_3"
        >
          LC3 assembly
        </a>
        , which I could watch each command as it was turned into binary and ran
        through the CPU.
      </p>
    </div>
  );
}
